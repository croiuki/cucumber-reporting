#macro(includeTitle, $reportName)

#set($buildNo = "")
#if($build_number)
  #set($buildNo = "(no $build_number)")
#end
##<title>Cucumber Reports $buildNo - $reportName</title>
<title>测试报告 $buildNo - $reportName</title>

#end
